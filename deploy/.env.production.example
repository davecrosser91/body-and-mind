# ===========================================
# Production Environment Variables
# Copy to /var/www/routine-game/.env
# ===========================================

# App
NODE_ENV=production
NEXTAUTH_URL=https://your-domain.com

# Database (use Neon for serverless PostgreSQL - recommended)
# Sign up at: https://neon.tech
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/routine_game?sslmode=require"

# Or use local PostgreSQL on VPS:
# DATABASE_URL="postgresql://routine_user:secure_password@localhost:5432/routine_game"

# Auth - Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-key-min-32-characters"

# Whoop Integration (optional)
WHOOP_CLIENT_ID="your-whoop-client-id"
WHOOP_CLIENT_SECRET="your-whoop-client-secret"
WHOOP_REDIRECT_URI="https://your-domain.com/api/v1/integrations/whoop/callback"
